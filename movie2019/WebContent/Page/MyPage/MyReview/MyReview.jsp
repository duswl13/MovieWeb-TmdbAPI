<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>마이페이지:내 리뷰 관리</title>
<style>
b {
	color: yellow;
	padding-right: 50px;
}
</style>
</head>
<body>
 <div class="container marketing">

      <!-- Three columns of text below the carousel -->
      <div class="row">
        <div class="col-lg-4">
          <img class="img-circle" src="data:image/gif;base64,R0lGODlhAQABAIAAAHd3dwAAACH5BAAAAAAALAAAAAABAAEAAAICRAEAOw==" alt="Generic placeholder image" width="140" height="140">
          <h2>제목</h2>
          <p>재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.</p>
          <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
        </div><!-- /.col-lg-4 -->
        <div class="col-lg-4">
          <img class="img-circle" src="data:image/gif;base64,R0lGODlhAQABAIAAAHd3dwAAACH5BAAAAAAALAAAAAABAAEAAAICRAEAOw==" alt="Generic placeholder image" width="140" height="140">
          <h2>제목</h2>
          <p>재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.</p>
          <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
        </div><!-- /.col-lg-4 -->
        <div class="col-lg-4">
          <img class="img-circle" src="data:image/gif;base64,R0lGODlhAQABAIAAAHd3dwAAACH5BAAAAAAALAAAAAABAAEAAAICRAEAOw==" alt="Generic placeholder image" width="140" height="140">
          <h2>제목</h2>
          <p>재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.재미있었다. 여기는 나중에 DB에서 값을 가져올거다.</p>
          <p><a class="btn btn-default" href="#" role="button">View details &raquo;</a></p>
        </div><!-- /.col-lg-4 -->
       
      </div><!-- /.row -->


    </div><!-- /.container -->
</body>
</html>